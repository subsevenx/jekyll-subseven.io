    <main class="container" id="content-container">
        <div class="align-items-center">
            <h1>{{page.title}}</h1>
        </div>
        <hr>
        {% if page.sidebar == true %}
            <div class="row">
                <div class="col-lg-9">
                    {%- if page.blurb -%}
                        {{page.blurb}}
                    {%- endif -%}
                    {{content}}
                </div>
                <div class="col-lg-3">
                    {% include components/sidebar/sidebar.liquid %}
                </div>
            </div>   
        {% else %} 
        {%- if page.blurb -%}
        {{page.blurb}}
        {%endif%}
            {{- content -}} 
        {% endif %}
    </main>